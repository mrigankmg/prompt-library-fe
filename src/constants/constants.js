export const THEME = {
  LIGHT: "light",
  DARK: "dark",
};

export const STORAGE_KEYS = {
  THEME: "theme",
  PROMPTS: "prompts",
  SESSION_USER_ID: "userSessionId",
};

/** Max items per page for prompt list and per-prompt notes list */
export const LIST_PAGE_SIZE = 10;
